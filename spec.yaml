name: zero2prod
region: tor
services:
  - name: zero2prod
    dockerfile_path: Dockerfile
    source_dir: src
    github:
      branch: master
      deploy_on_push: true
      repo: sunnywhy/zero2prod
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xs
    routes:
      - path : /
